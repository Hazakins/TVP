import React from 'react';

const PickleballIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  // Empty but valid SVG component to prevent parsing errors
  <svg {...props} />
);

export default PickleballIcon;
